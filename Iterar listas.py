#4. Iterar Listas

#Utilize um loop for para exibir os elementos da lista ['a', 'b', 'c', 'd', 'e'].

lista = ['a', 'b', 'c', 'd', 'e']

for elemento in lista:
    print(elemento)

#Dada a lista de números [5, 10, 15, 20, 25], crie uma nova lista com o 
# dobro dos valores usando um loop.

numeros = [5, 10, 15, 20, 25]

for numero in numeros:
    print(numero*2)